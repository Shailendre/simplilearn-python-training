
# importing modules
# basically modules are nothing but file names
# and we can import the content within the module(file)

# usually we can import modules within
# - same directory
# - <pyhton localtion>/Lib

# here classes is python file within same directory
from classes import calculator as c

print (c.mul(2,3))
